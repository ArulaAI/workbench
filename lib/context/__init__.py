"""SPEED Context Constructor — Layers 1-3.

Layer 1: Pre-computed codebase index (project map, CSG, skeletons, spec alignment)
Layer 2: Task-specific projection (code context, task context, spec context, budgeting)
Layer 3: Stage-specific assembly (per-agent prompt formatting)
"""
