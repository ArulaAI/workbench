# Test-only dependencies.
#
#   python3 -m pip install -r requirements.txt -r requirements-dev.txt
#
# The suites need Python 3.11 or newer, matching SPEED's own runtime floor
# (config parsing uses tomllib).
pytest>=8.0
